const INITIAL_STATE = {}

const ProfileReducer = (state = INITIAL_STATE, action) => {
  const { type } = action

  switch (type) {
    default:
      return state
  }
}

export default ProfileReducer
